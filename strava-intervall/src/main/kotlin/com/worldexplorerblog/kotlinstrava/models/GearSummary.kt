package com.worldexplorerblog.kotlinstrava.models

data class GearSummary(val id: String,
                       val primary: Boolean,
                       val name: String,
                       val distance: Int)